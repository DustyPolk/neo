# API key for DeepSeek or compatible OpenAI endpoint
DEEPSEEK_API_KEY="your_api_key_here"

# Optional: If you need to override the base URL (though it's hardcoded in InitializeAIClient for now)
# OPENAI_BASE_URL="https://api.example.com"
